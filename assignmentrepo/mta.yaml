_schema-version: "3.2"
ID: assignmentrepo
description: A Fiori application.
version: 0.0.1
modules:
- name: assignmentrepo-dest-content
  type: com.sap.application.content
  requires:
  - name: assignmentrepo-dest-srv
    parameters:
      content-target: true
  - name: assignmentrepo-repo-host
    parameters:
      service-key:
        name: assignmentrepo-repo-host-key
  - name: assignmentrepo-uaa
    parameters:
      service-key:
        name: assignmentrepo-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: assignmentrepo_repo_host
          ServiceInstanceName: assignmentrepo-html5-srv
          ServiceKeyName: assignmentrepo-repo-host-key
          sap.cloud.service: assignmentrepo
        - Authentication: OAuth2UserTokenExchange
          Name: assignmentrepo_uaa
          ServiceInstanceName: assignmentrepo-xsuaa-srv
          ServiceKeyName: assignmentrepo-uaa-key
          sap.cloud.service: assignmentrepo
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: assignmentrepo-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: assignmentrepo-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - assignmentrepo.zip
      name: assignmentrepo
      target-path: resources/
- name: assignmentrepo
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: assignmentrepo-dest-srv
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: assignmentrepo-dest-srv
    service-plan: lite
- name: assignmentrepo-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: assignmentrepo-xsuaa-srv
    service-plan: application
- name: assignmentrepo-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: assignmentrepo-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
